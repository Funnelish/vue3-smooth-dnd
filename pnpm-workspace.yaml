packages:
  - packages/*

# `example` pulls in esbuild via Vite. CI installs with --ignore-scripts and
# never builds the example, so this only matters for local development.
onlyBuiltDependencies:
  - esbuild
